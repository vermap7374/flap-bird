# ================================
#          Game Configurations
# ================================

# Screen dimensions
WIDTH, HEIGHT = 400, 600  # Width and height of the game window (pixels)

# Bird physics
GRAVITY = 0.5  # Force pulling the bird downwards (affects fall speed)
JUMP_STRENGTH = -8  # Initial upward velocity when the bird jumps (negative = upward movement)

# Pipe settings
PIPE_SPEED = 3  # Speed at which pipes move left (affects difficulty)
PIPE_GAP = 150  # Vertical space between top and bottom pipes (affects difficulty)
PIPE_WIDTH = 80  # Width of each pipe in pixels

# Bird position
BIRD_X_POS = 50  # Horizontal position of the bird (constant, only moves vertically)
